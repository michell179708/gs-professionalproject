// create an algorithm of impar number 
 
// we create a variable that will count our loop.
// we inicialite that variable in cero to count from one.
 let i = 0;

 while( i <= 10){
    if(i % 2 !== 0 ){
        console.log('impart', i);
    }
    i++;
 }